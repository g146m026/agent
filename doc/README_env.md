# README gestion des secrets/env

Les tokens, secrets et variables d’environnement nécessaires à l’exécution des tests et scripts doivent être placés dans un fichier `.env` (non versionné) à la racine du dossier `agent/`.
Documenter chaque variable dans ce fichier et référencer la procédure d’obtention/rotation dans ce README.
